// apps/api/src/routes/payment.js — TODO: migrate from backend/src/routes/payment.js
const router = require('express').Router();
// Stub — replace with full implementation per DevSpec
router.get('/', (_req, res) => res.json({ success: true, message: 'payment routes — coming soon' }));
module.exports = router;
