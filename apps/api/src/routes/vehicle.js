// apps/api/src/routes/vehicle.js — TODO: migrate from backend/src/routes/vehicle.js
const router = require('express').Router();
// Stub — replace with full implementation per DevSpec
router.get('/', (_req, res) => res.json({ success: true, message: 'vehicle routes — coming soon' }));
module.exports = router;
