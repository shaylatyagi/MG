// apps/api/src/routes/kyc.js — TODO: migrate from backend/src/routes/kyc.js
const router = require('express').Router();
// Stub — replace with full implementation per DevSpec
router.get('/', (_req, res) => res.json({ success: true, message: 'kyc routes — coming soon' }));
module.exports = router;
