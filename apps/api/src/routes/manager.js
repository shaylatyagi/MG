// apps/api/src/routes/manager.js — TODO: migrate from backend/src/routes/manager.js
const router = require('express').Router();
// Stub — replace with full implementation per DevSpec
router.get('/', (_req, res) => res.json({ success: true, message: 'manager routes — coming soon' }));
module.exports = router;
