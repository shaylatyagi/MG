// apps/api/src/routes/collection.js — TODO: migrate from backend/src/routes/collection.js
const router = require('express').Router();
// Stub — replace with full implementation per DevSpec
router.get('/', (_req, res) => res.json({ success: true, message: 'collection routes — coming soon' }));
module.exports = router;
