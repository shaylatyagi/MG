// apps/api/src/routes/owner.js — TODO: migrate from backend/src/routes/owner.js
const router = require('express').Router();
// Stub — replace with full implementation per DevSpec
router.get('/', (_req, res) => res.json({ success: true, message: 'owner routes — coming soon' }));
module.exports = router;
