// apps/api/src/routes/assignment.js — TODO: migrate from backend/src/routes/assignment.js
const router = require('express').Router();
// Stub — replace with full implementation per DevSpec
router.get('/', (_req, res) => res.json({ success: true, message: 'assignment routes — coming soon' }));
module.exports = router;
